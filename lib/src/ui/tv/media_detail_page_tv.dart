library;
import 'package:flutter/material.dart';
class MediaDetailPageTv extends StatelessWidget {
  final int mediaId;
  final String? displayTitle;
  const MediaDetailPageTv({super.key, required this.mediaId, this.displayTitle});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
